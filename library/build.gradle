apply plugin: 'com.android.library'

android {
    compileSdkVersion 29

    defaultConfig {
        minSdkVersion 21
        targetSdkVersion 29
        versionCode 30
        versionName "1.30"
    }

    buildTypes {
        release {
            minifyEnabled true
        }
    }
}