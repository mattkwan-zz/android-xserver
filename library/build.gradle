apply plugin: 'com.android.library'

android {
    compileSdkVersion 29

    defaultConfig {
        minSdkVersion 23
        targetSdkVersion 29
        versionCode 26
        versionName "1.26"
    }

    buildTypes {
        release {
            minifyEnabled false
        }
    }
}