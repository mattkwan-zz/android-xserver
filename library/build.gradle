apply plugin: 'com.android.library'

android {
    compileSdkVersion 29

    defaultConfig {
        minSdkVersion 23
        targetSdkVersion 29
        versionCode 22
        versionName "1.22"
    }

    buildTypes {
        release {
            minifyEnabled false
        }
    }
}