- Fixes keycodes. Also make Softkeyboard backspace work and avoid taking up the whole screen in landscape mode.
- Use touch inputs for mouseclicks (can be disabled via menu entry).
- Add simple way to execute binary applications on start.
- Add a simple window manager.
- Allow configuration of screen orientation vie menu entry (restarts activity and disconnects all clients).
- Made FLTK based programms properly work (app freezes ware caused by broken GContext SetDashes implementation.).
